export const settings = {
    rotatingText: {
        containerSelector: ".rotating-txt",
        wordsSelector: ".word",
        delay: 3000, // Délai entre les rotations en millisecondes.
        animationDuration: 500, // Durée de l'animation en millisecondes.
        distance: 100 // Distance de déplacement en pourcentage.
    }
};
